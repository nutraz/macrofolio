# Scripts

This folder is intentionally minimal for judging.

- Web app: `macrofinal/web` (run `npm run dev`)
- Mobile app: `macrofinal/mobile` (run `npm run android` / `npm run ios`)

Legacy/experimental build scripts from earlier iterations live in `macrofinal/legacy/scripts/`.

