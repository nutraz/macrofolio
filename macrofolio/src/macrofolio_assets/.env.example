# =============================================================================
# Macrofolio Environment Variables Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control - it's in .gitignore
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION (Required)
# -----------------------------------------------------------------------------
# Get these from: Supabase Dashboard → Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# -----------------------------------------------------------------------------
# SMART CONTRACT CONFIGURATION (Required for Live Mode)
# -----------------------------------------------------------------------------
# Deploy PortfolioAnchor.sol to Polygon Amoy (Chain ID: 80002)
# Get contract address after deployment via Remix IDE
VITE_CONTRACT_ADDRESS=0x...

# -----------------------------------------------------------------------------
# NETWORK CONFIGURATION
# -----------------------------------------------------------------------------
# Polygon Amoy Testnet
VITE_CHAIN_ID=80002
VITE_RPC_URL=https://rpc-amoy.polygon.technology
VITE_EXPLORER_URL=https://www.amoy.polygonscan.com
VITE_NETWORK_NAME=Polygon Amoy

# -----------------------------------------------------------------------------
# FEATURE FLAGS
# -----------------------------------------------------------------------------
# DEMO MODE: Set to "true" for testing without real blockchain transactions
# DEFAULT: "false" (Live Mode)
# 
# When false:
#   - Real MetaMask wallet connection required
#   - Real blockchain transactions on Polygon Amoy
#   - Data saved to Supabase database
#   - No "Demo Mode" banner
#
# When true:
#   - Simulated wallet connection (demo-user)
#   - No real blockchain transactions
#   - Demo data displayed
#   - "Demo Mode" watermark banner shown
VITE_DEMO_MODE=false

# Analytics: Enable anonymous usage tracking
VITE_ENABLE_ANALYTICS=false

# -----------------------------------------------------------------------------
# LOCAL DEVELOPMENT NOTES
# -----------------------------------------------------------------------------
# 1. cp .env.example .env
# 2. Edit .env with your values
# 3. npm run dev
#
# For Netlify deployment:
#   - Add these variables to Site Settings → Environment Variables
#   - Set VITE_DEMO_MODE=false for production
#
# Security:
#   - All VITE_* variables are exposed to the browser
#   - Never put private keys or secrets in VITE_* variables
#   - Use Supabase service role key server-side only
# =============================================================================

