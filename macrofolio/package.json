{
  "name": "macrofolio-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "src/macrofolio_assets"
  ],
  "scripts": {
    "test:contracts": "cd test && npx hardhat test",
    "test:frontend": "cd src/macrofolio_assets && npm test",
    "test": "npm run test:contracts && npm run test:frontend",
    "install:all": "npm install && cd src/macrofolio_assets && npm install"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  }
}
